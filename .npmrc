# El registry configurado en cada máquina (p. ej. Artifactory corporativo) sustituye
# al del lock, y en CI el lock usa el registry público. Evita E401 en GitHub Actions.
replace-registry-host=always
